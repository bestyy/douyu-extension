# 直播开播通知扩展

一个 Chrome MV3 浏览器扩展：监控用户关注的直播平台房间（斗鱼 / B站）的开播状态，开播时发桌面通知，popup 中展示在线主播列表。扩展不登录任何平台账号，全部使用匿名公开接口轮询。

## Language

**平台 (platform)**:
直播平台。取值 `douyu` / `bilibili`。房间、主播、通知、设置开关均以平台为第一维划分。
_Avoid_: 站点、渠道（渠道专指 B站弹幕的传输通道）

**房间 (room)**:
用户在设置页添加、扩展持续监控的一个直播房间。唯一标识是 `platform + roomId` 复合键。对应存储中的 `rooms` 数组。
_Avoid_: 直播间（口语可以，代码/文档用"房间"）

**主播 (streamer)**:
某个房间当前的开播信息快照：是否在线、昵称、标题、封面、分类等。对应存储中的 `streamers` 数组，由轮询产出、由弹幕采样补充观众数字段。
_Avoid_: 用户（主播不是"用户"，扩展没有用户概念）

**房间库 (room store)**:
拥有 `rooms`、`streamers`、`settings` 与分类列表四个键的形状与全部变更的概念：轮询结果合并、采样值到达、连续两轮离线后的观众数清空、房间增删与重排、分类增删改名与归类、旧格式迁移、平台观众数开关判定。它是这四个键的唯一写入者——页面读取它的只读快照，变更交由它执行。
_Avoid_: 存储层、storage 封装（那是 chrome.storage 的转发，房间库拥有的是房间、主播与分类的形状）

**房间号 (roomId)**:
用户在设置页输入的房间编号，字符串形式，必须是纯数字。
_Avoid_: ID、房间 ID

**房间标识 (room identity)**:
`platform + roomId` 组成的复合键（字符串形式 `platform_roomId`），以及由平台决定的房间事实：展示标签（斗鱼 / B站）、观众数指标文案与存储字段、平台观众数开关、直播间 URL。房间身份的一切查询——拼键、拆键、旧格式（房间条目缺 `platform`）兜底、房间号校验——都走它，存储与判定只接收它的结论；它内部的表不对外暴露，只暴露查询。归属于 `lib/room-identity.js`，是零依赖的词汇 module，加载排在所有使用它的 module 之前（平台事实的归属判据见 ADR-0005）。
_Avoid_: 房间 ID（房间号是用户输入的纯数字，房间标识是它加上平台）、主键（那是存储术语）、平台表（表是它的内部结构，不是它对外的东西）

**分类 (category)**:
用户自建的房间组织维度：跨平台、互斥（一个房间至多属于一个分类）、纯组织（不影响任何通知行为，与 `notify` / `watch` / `viewerAlert` / `surgeAlert` / `highlightAlert` 全部正交）。它是一等实体——单独一份分类列表（名称 + 顺序），房间只记 `rooms[].categoryId`；名称唯一、trim 后非空、不得等于「未分类」。它是「房间、主播、通知、设置开关均以平台为第一维划分」这条约定的唯一例外：一个分类可以同时装斗鱼与 B 站房间。存储与全部变更归房间库（见 ADR-0008）。
_Avoid_: 分组、标签、类别、文件夹（标签是可以叠加的多对多维度，分类是互斥的；分组是它的一种实现，不是它的名字）

**未分类 (uncategorized)**:
房间没有分类时的展示形态。它不是分类：不落盘、不在分类列表里、不能改名 / 删除 / 拖拽，在弹窗与设置页都固定排在最前。`rooms[].categoryId` 为空即未分类，分类列表的保留名「未分类」也不允许被建成真分类。
_Avoid_: 默认分类、其他、无分组（它有确定的名字和固定的位置，不是一句「没有分类」的兜底文案）

**分类顺序 (category order)**:
分类之间的先后，就是分类列表本身的顺序，由用户拖拽分类标题调整；「未分类」不在其中、固定在最前。
_Avoid_: 排序权重、sortIndex（顺序是列表自身的属性，不是每个分类上的一个数字）

**分类内房间顺序 (room order within a category)**:
每个分类内部各自保留一份房间的相对顺序，互不影响——不再存在「全体房间一份顺序」这回事。换分类、删分类后回落未分类、新建房间，一律追加到目标分组的末尾（同一条落点规则，见 ADR-0008）。
_Avoid_: 全局顺序、拖拽顺序（拖拽是它的操作方式，不是它本身）

**开播状态 (online)**:
某个房间当前是否在直播。斗鱼的口径由 `lib/douyu-api.js` 的 `fetchRoomInfo` 折算（`show_status` 为数字或字符串 `1`、且 `videoLoop !== 1`），是「什么算开播」的唯一实现；轮询合并（`streamers[].online`）与弹幕客户端的采样超时兜底（未开播就按贵宾数 0 上报，在播则重连一次，查不到则本轮放弃）都消费它的结论，不各自再判一遍。它不属于房间标识 module：房间标识装的是平台常量事实（标签、指标名、存储字段、URL），开播状态是每轮轮询的观测量。
_Avoid_: 在线状态、直播中（状态是观测值，不是事件）

**贵宾数 (vipCount)**:
斗鱼直播间的贵宾（付费礼物用户）数量，通过弹幕 WebSocket 的 `oni` 消息采样获取，约每 6 秒推送一次。存储在 `streamers[].vipCount`。
_Avoid_: 观众数（贵宾数是斗鱼特有指标）

**高能榜 (rankCount)**:
B站直播间的高能榜在线数，通过弹幕 WebSocket 的 `ONLINE_RANK_COUNT` 消息采样获取。存储在 `streamers[].rankCount`。
_Avoid_: 观众数（高能榜是 B站特有指标）

**观众数 (viewer count)**:
各平台特有观众指标的统称：斗鱼贵宾数、B站高能榜。通过 10 分钟一次的弹幕 WebSocket 采样获取，每个平台独立开关控制（`settings.fetchDouyuViewerCount` / `fetchBilibiliViewerCount`）。
_Avoid_: 在线人数（语义不同）

**观众数提醒 (viewer alert)**:
某房间的观众数从阈值以下升到阈值以上时发一条桌面通知的 per-room 功能（`rooms[].viewerAlert`），受全局总开关 `settings.viewerAlertEnabled` 控制（默认开启，关闭后不判定、各房配置保留）。判定依据是观众数采样结果，因此要求对应平台的观众数开关处于开启状态。「要不要发这一条」的判定链（全局总开关 → 该房配置归一化 → 上升边沿）整条归 `lib/viewer-alert.js` 的纯裁决 `decideViewerAlert`，编排只在值到达处触发（见 ADR-0002）；平台观众数开关不是这条链的一环，它是写入门——关闭时数值根本不落盘，归房间库（`recordViewerCount`）。两道门各管一件事，不是同一判定的两处实现，不要合并。与开播通知的 `notify`、弹幕检测的 `watch` 相互正交。
_Avoid_: 阈值告警、贵宾数监控（提醒是越过阈值的一次性通知，不是持续监控）

**采样 (sample)**:
每 10 分钟由 `chrome.alarms` 驱动的一次短连接弹幕 WebSocket 会话：连接 → 收到目标数据 → 立即断开。采样自身不保持连接。
_Avoid_: 长连接（长连接属于弹幕检测的检测长连接，见 ADR-0001）

**开播通知 (notification)**:
新开播时发送的桌面通知，格式 `[平台] 昵称 开播了！`。按房间可单独关闭（`rooms[].notify`）。
_Avoid_: 提醒、弹窗

**通道 (channel)**:
B站弹幕采样的传输方式，取值 `SW 直连` 或 `页面桥接`。登录态下 SW 直连握手必被风控，需页面桥接；未登录时 SW 直连可用。状态与编排封装在 BiliBridgeChannel。
_Avoid_: 平台、渠道

**弹幕客户端 (barrage client)**:
与平台弹幕服务器建 WebSocket 连接的客户端（`BarrageClient` / `BilibiliBarrageClient`）：采样模式解析 `oni` / `ONLINE_RANK_COUNT` 回调观众数字段，检测模式解析 `chatmsg` / `DANMU_MSG` 回调弹幕文本（同一条连接同时供给弹幕检测与弹幕激增）。两种模式各用独立实例（采样短连与检测长连接并存，见 ADR-0001）。
_Avoid_: WS 客户端（弹幕客户端是领域概念，WS 是技术细节）

**检测词 (keyword)**:
某房间启用的弹幕命中词，一房多词共用一个计数器，任一词命中即计数加一。子串包含、不区分大小写。
_Avoid_: 关键词、关键字、监控词

**弹幕检测 (danmaku watch)**:
对开启的房间在开播期间保持弹幕长连接、按滑动窗口计数命中、触发后进冷却的 per-room 功能，受全局总开关 `settings.danmakuWatchEnabled` 控制（默认开启；关闭后不再盯守任何房间、不排队、不发检测通知，各房已配的检测词保留）。与开播通知的 `notify` 正交，计数在下播时清空。盯守期间的长连接同时供弹幕激增统计条数，因此两个功能共用同一份盯守名额（见 ADR-0004）。
_Avoid_: 弹幕监控（监控指轮询开播状态，不是看弹幕文本）、关键词告警

**检测通知 (detection notification)**:
命中在窗口内达到阈值时发的桌面通知，按房间复用通知 ID 覆盖，不堆积，点击进入直播间。
_Avoid_: 提醒、弹窗、告警

**观众数通知 (viewer alert notification)**:
观众数越过房间阈值时发的桌面通知（ID 为房间复合键 + `_viewer` 后缀），正文报越过阈值时的当前观众数，房间标题放上下文行，按房间复用通知 ID 覆盖，不堆积，点击进入直播间。
_Avoid_: 提醒、弹窗、告警

**弹幕激增提醒 (danmaku surge alert)**:
某房间的弹幕条数相对该房本场自身的基线突然异常变多时发一条桌面通知的 per-room 功能（`rooms[].surgeAlert`，纯布尔、无 per-room 参数），受全局总开关 `settings.surgeAlertEnabled` 控制（默认开启；关闭后不判定，各房开关保留）。阈值参数是全局的：倍数 `settings.surgeMultiple`、基线门槛 `settings.surgeMinBaseline`、冷启动时长 `settings.surgeMinBuckets`（开播后攒够这么多个完整分钟桶才开始判定，防开播爬坡误报）、冷却 `settings.surgeCooldownMinutes`。判定数据源是弹幕检测的长连接，所以只在房间开播且排进盯守名额时有效，与弹幕检测共用同一份盯守名额（见 ADR-0004）；基线与冷却在下播时清空，下一场重新积累。与开播通知的 `notify`、弹幕检测的 `watch`、观众数提醒的 `viewerAlert` 相互正交（各自的总开关互不影响）。
_Avoid_: 热点提醒（热点是推论不是观测）、弹幕量监控（监控指轮询开播状态）

**激增通知 (surge notification)**:
弹幕激增提醒发出的桌面通知（ID 为房间复合键 + `_surge` 后缀），正文首行报上一分钟的弹幕条数与平时水位，第二行是样本弹幕，房间标题放上下文行（Windows 上上下文行不渲染，正文才保证看得见），按房间复用通知 ID 覆盖，不堆积，点击进入直播间。
_Avoid_: 提醒、弹窗、告警

**样本弹幕 (surge sample)**:
附在激增通知正文里的那一条弹幕，取自被裁决的那一分钟（`「…」` 包裹），用来回答「现在在聊什么」。评选口径是那一分钟里最长的合格弹幕：至少 4 个字符且不同字符不少于 3 个，因此复读刷屏（`666666`、`哈哈哈哈`）与短句都不入选；一分钟里没有合格候选就不发这一行，通知只报条数。取样与条数无关，不影响激增判定。
_Avoid_: 代表弹幕、热门弹幕（样本是本地挑选的观测值，不是平台的统计结果）

**看点 (highlight)**:
斗鱼为直播中的房间自动切出的精彩片段，标题、起止时间、热度都由平台给（不是我们判断出来的精彩时刻）。房间未开播时拿不到——实测下播房间的看点列表为空；直播中也可能长期一条都没有。只有斗鱼有这个形态的信息。
_Avoid_: 高光、精彩片段、集锦、highlight

**看点通知 (highlight notification)**:
某房间出现新看点时发的桌面通知（ID 为房间复合键 + `_highlight` 后缀），正文报该条看点的标题，房间标题放上下文行，按房间复用通知 ID 覆盖，不堆积，点击进入直播间。逐房由 `rooms[].highlightAlert`（纯布尔、默认关）决定，受全局总开关 `settings.highlightAlertEnabled`（默认开启）控制，与开播通知的 `notify`、弹幕检测的 `watch`、观众数提醒的 `viewerAlert`、弹幕激增提醒的 `surgeAlert` 相互正交。取数是每 5 分钟一次的接口轮询，不新增连接、不占盯守名额；看点由平台事后切出，通知天然晚于直播画面几分钟。
_Avoid_: 提醒、弹窗、高光通知

**看点水位 (highlight watermark)**:
某个房间「已经就看点通知到哪一条」的记账（判据是平台给每条看点的递增编号，不是时间）：首次观测一个房间的看点（刚打开该房开关、刚加入房间）时只把当前最新的一条记为水位、不发通知；此后编号大于水位的才算新看点，一次发现多条只报最新那一条，其余在水位里一并消化。水位只增不减，房间被移除或重新添加时清掉。
_Avoid_: 已读、游标、lastId（水位记的是「通知过」，不是「用户看过」）

**今日统计 (today stats)**:
某个房间**当天 0 点起累计**的四项数字——弹幕数（弹幕人次，同一人可重复计）、弹幕人数（去重发弹幕人数）、礼物金额（付费礼物金额，单位元）、礼物人数（付费送礼人数）。它由第三方数据站 doseeing 的区间聚合直接给出（我们是消费方，不是计算方），扩展按「开播中的斗鱼房间」每 5 分钟取一次，只读展示在弹窗的在线列表里，是本项目唯一不产生通知的功能。四项口径互不换算，也不得与相邻术语混同：它不是「观众数」（那是当前在房的贵宾数 / 高能榜这类指标），「礼物人数」不是「贵宾数」（后者是当前在房的付费用户数、由弹幕采样得到），「弹幕数」不是弹幕激增的「条数」（后者是本地检测长连接的观测值，与第三方的整房统计必然对不上）。只有斗鱼房间有这个形态的数据（平台门 `STATS_PLATFORMS`），全局总开关为 `settings.todayStatsEnabled`，逐房结果落在编排自己持有的 `todayStats` 键里。
_Avoid_: 人气数据、热度、观众数、直播数据（「数据 / 统计」单用指代不明，四项各有名字，不要合称）
